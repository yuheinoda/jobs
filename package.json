{
  "name": "resume-pages",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint:text": "textlint \"docs/README.md\"",
    "lint:md": "markdownlint \"docs/README.md\"",
    "lint": "npm run lint:text && npm run lint:md",
    "setup:jekyll": "cd docs && bundle config set --local path 'vendor/bundle' && bundle install",
    "preview": "cd docs && bundle exec jekyll serve",
    "preview:host": "cd docs && bundle exec jekyll serve --host 0.0.0.0"
  },
  "devDependencies": {
    "markdownlint-cli": "^0.39.0",
    "textlint": "^13.4.1",
    "textlint-rule-preset-japanese": "^10.0.1"
  }
}
